<Page x:Class="NguyenVanCau_SE18D07_A01.Views.Pages.CustomerManagementPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Admin Dashboard - Customer Management" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox x:Name="txtSearch" Width="200" Margin="0,0,10,0"/>
            <Button Content="Search" Width="80" Margin="0,0,5,0" Click="btnSearch_Click"/>
            <Button Content="Add" Width="80" Margin="0,0,5,0" Click="btnAddCustomer_Click"/>
        </StackPanel>

        <DataGrid x:Name="dgCustomers" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" Margin="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding CustomerID}" Width="50"/>
                <DataGridTextColumn Header="Name" Binding="{Binding CustomerFullName}" Width="150"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding Telephone}" Width="100"/>
                <DataGridTextColumn Header="Email" Binding="{Binding EmailAddress}" Width="200"/>
                <DataGridTextColumn Header="Birthday" Binding="{Binding CustomerBirthday, StringFormat=MM/dd/yyyy}" Width="120"/>
                <DataGridTemplateColumn Header="Actions" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Edit" Width="50" Margin="5,0" Click="btnEdit_Click"/>
                                <Button Content="Delete" Width="60" Margin="5,0" Click="btnDelete_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
