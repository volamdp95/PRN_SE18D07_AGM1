<Page x:Class="NguyenVanCau_SE18D07_A01.Views.Pages.RoomManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Room Management">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox x:Name="txtSearch" Width="200" Margin="0,0,10,0"/>
            <Button Content="Search" Click="btnSearch_Click" Width="80" Margin="0,0,5,0"/>
            <Button Content="Add" Click="btnAdd_Click" Width="80" Margin="0,0,5,0"/>
        </StackPanel>

        <DataGrid x:Name="dgRooms" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding RoomID}" Width="50"/>
                <DataGridTextColumn Header="Number" Binding="{Binding RoomNumber}" Width="100"/>
                <DataGridTextColumn Header="Description" Binding="{Binding RoomDescription}" Width="200"/>
                <DataGridTextColumn Header="Capacity" Binding="{Binding RoomMaxCapacity}" Width="100"/>
                <DataGridTextColumn Header="Price" Binding="{Binding RoomPricePerDate}" Width="100"/>
                <DataGridTemplateColumn Header="Actions" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Edit" Click="btnEdit_Click" Width="50" Margin="5,0"/>
                                <Button Content="Delete" Click="btnDelete_Click" Width="60" Margin="5,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
